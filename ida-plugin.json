{
  "IDAMetadataDescriptorVersion": 1,
  "plugin": {
    "name": "ida-chat",
    "entryPoint": "ida_chat_plugin.py",
    "version": "0.2.6",
    "idaVersions": ["9.0", "9.1", "9.2", "9.3"],
    "description": "LLM Chat Client for IDA Pro - Multi-provider reverse engineering assistant (Claude, Gemini, OpenAI, OpenRouter, Ollama, NVIDIA NIM) embedded directly in IDA.",
    "license": "MIT",
    "categories": ["collaboration-and-productivity", "ui-ux-and-visualization"],
    "pythonDependencies": [
      "claude-agent-sdk",
      "ida-domain",
      "ida-settings",
      "claude-code-transcripts"
    ],
    "urls": {
      "repository": "https://github.com/HexRaysSA/ida-chat-plugin"
    },
    "authors": [{
      "name": "Hex-Rays",
      "email": "support@hex-rays.com"
    }],
    "maintainers": [{
      "name": "Pierre-Alexandre Losson",
      "email": "palosson@hex-rays.com"
    }],
    "settings": [
      {
        "key": "show_wizard",
        "name": "Show Setup Wizard",
        "type": "boolean",
        "description": "Show the setup wizard on plugin launch",
        "default": true,
        "required": false,
        "prompt": false
      },
      {
        "key": "provider_profiles",
        "name": "Provider Profiles",
        "type": "string",
        "description": "Internal JSON storage for provider credentials and model preferences",
        "default": "{}",
        "sensitive": true,
        "required": false,
        "prompt": false
      },
      {
        "key": "provider",
        "name": "LLM Provider",
        "type": "string",
        "description": "Provider: claude, gemini, openai, openrouter, ollama, nim",
        "default": "claude",
        "required": false
      },
      {
        "key": "auth_mode",
        "name": "Authentication Mode",
        "type": "string",
        "description": "Authentication mode: system (Claude only) or api_key",
        "default": "system",
        "required": false
      },
      {
        "key": "api_key",
        "name": "API Key",
        "type": "string",
        "description": "Provider API key/token (optional for Ollama local mode)",
        "sensitive": true,
        "required": false
      },
      {
        "key": "model",
        "name": "Model Override",
        "type": "string",
        "description": "Optional model override for the selected provider",
        "required": false
      },
      {
        "key": "base_url",
        "name": "Base URL Override",
        "type": "string",
        "description": "Optional base URL override (OpenAI-compatible endpoints supported)",
        "required": false
      },
      {
        "key": "auth_type",
        "name": "Legacy Authentication Type",
        "type": "string",
        "description": "Legacy compatibility setting from older plugin versions",
        "default": "system",
        "required": false,
        "prompt": false
      }
    ]
  }
}
